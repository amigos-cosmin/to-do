import styles from "../styles/CustomComponent.module.css"
export default function CustomComponent(){
    return(
        <div>
        <h1 className={styles.title}>Simple to-do app</h1>
        </div>
    );
}